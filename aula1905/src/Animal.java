public interface Animal { //Não usa o termo class
    /*todas as classes que IMPLEMENTS esta interface devem escrever (implementar) 
    os métodos com "assinaturas" definidas abaixo*/
    void som();
    void mover();
}